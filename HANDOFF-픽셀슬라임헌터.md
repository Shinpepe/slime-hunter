# 픽셀 슬라임 헌터 — 인수인계 문서 (2026-08-20 기준)

## 이 문서의 용도
시크릿 채팅으로 개발했기 때문에 대화 기록이 보존되지 않습니다. 다음 대화에서 개발을 이어가려면:

1. 새 대화에 **이 문서 + `pixel-slime-hunter.html` + `harness7.js`** 3개 파일을 업로드
2. "이 인수인계 문서 기준으로 게임 개발을 이어가줘"라고 요청
3. 테스트 실행: `node harness7.js /mnt/user-data/uploads/pixel-slime-hunter.html` (36개 테스트 전부 PASS가 정상)

수정 워크플로: 코드 수정 → `node --check`로 문법 확인 → harness7 실행 → 필요 시 테스트 추가. 지금까지 이 방식으로 모든 기능을 검증하며 개발했음.

## 게임 개요
- 단일 HTML 파일, 내부 해상도 320x180 픽셀아트(모든 스프라이트는 코드로 그림), 한국어 UI
- 조작: 방향키/WASD 이동, SPACE·Z·J 공격(꾹 누르기 허용), SHIFT·X 대시, P 일시정지, M 음소거, R 재시작. 모바일 터치(조이스틱+공격+대시 버튼) 지원
- 구조: 4스테이지 × 5웨이브 → 클리어 시 다음 스테이지, 화산(4) 클리어 시 2회차(NG+) 무한 루프
- 스테이지: 초원(장애물 없음) → 사막(선인장 3~5) → 설원(빙판 2~3) → 화산(용암 2)
- 몬스터: green(hp2/10점) · red(hp3/25) · gold(hp5/60) · bat(비행 hp1/15) · sand(원거리 침, 사막부터) · frost(피격 시 감속, 설원부터) · bomb(기폭 폭발, 화산부터). blue는 sand로 대체되어 삭제됨

## 사용자가 확정한 설계 규칙 (변경 시 반드시 사용자와 상의)
- **대시는 사기 방지 설계**: 대시 중+그레이스 0.1초 동안 몬스터 몸통과 지형(선인장·용암)은 통과하지만, 투사체와 폭발은 맞음. 완전 무적 아님
- **꾹 누르기 스턴락 금지**: 연속 타격 시 넉백저항 누적 — `kb = 115*kbMul/(1+kbRes*1.1)`, kbRes 최대 4, `hitRecent` 0.9초 내 재타격 시 +1. 황금 슬라임은 제자리 꾹 누르기를 뚫고 접근해야 하고, 정면 접근 초록 1마리는 안전 처치 가능해야 함 (둘 다 테스트로 고정됨)
- **지형 유도킬은 공짜가 아님**: 몬스터는 점프 각도를 7개 후보(±0.55/1.1/1.7rad)로 틀어 위험 지형 회피(전부 막히면 원각도 = 가끔 실수). 넉백은 회피 무시 → 조준 실력으로만 지형킬 가능. 지형킬 점수는 **50%**(회색 팝업). 선인장은 몬스터가 3번 박히면 파괴
- **장애물은 웨이브마다 전부 재배치**: 거절 샘플링 — 상호 중심거리 ≥ max(40, 반경합+26), 벽 여백 16px, 플레이어 45px, 면적 상한(용암·선인장 15%, 빙판 25%), 60회 실패 시 개수 축소. 등장 연출 0.4초 동안은 무해
- **캔버스 텍스트는 자체 비트맵 폰트**(A–Z, 0–9, +, -; M은 5칸 가변폭) — fillText 흐림 문제 때문. HUD는 `STAGE 1-3`, 회차는 `X2` 접두

## 시스템 상세 (구현·검증 완료)
- **용암**: 몹 즉사(박쥐·점프 중 z>2·기폭 중 면제). 플레이어는 2피해 + 최근접 변으로 사출. **사출은 무적시간과 무관하게 항상** 발동(용암 안에 서 있는 함정 방지). 최대 폭 40px — 대시 40px+그레이스로 횡단 가능하게 맞춘 수치
- **빙판**: 플레이어 이동에 관성(가속 4.2/s, 평지는 즉시), 넉백 감쇠 빙판 0.08 / 평지 0.0008, 슬라임 착지 시 점프 속도의 80%가 미끄러짐으로 이월
- **폭탄 슬라임**: 처치 시 즉사 대신 기폭 1.2초(점수는 즉시 지급) → 폭발(플레이어 반경 26에 1피해·무적 적용, 몹 반경 30에 2피해 → 연쇄 기폭 가능). 기폭 중엔 스윙·접촉·용암 판정 전부 제외. 지형킬로 죽으면 폭발 없이 소멸
- **서리 슬라임**: 피격 시 `player.slowT=2`, 이동속도 0.7배, 눈송이 파티클 표시
- **모래 슬라임**: 거리 42~165에서 침 발사(0.38초 예비동작), 침·궤적 색은 몬스터 색상 연동. 침은 지형 위를 날아감(선인장 뒤 캠핑 방지)
- **스폰**: 동시 출현 상한 `MOB_CAP=9`(초과분 대기열), 간격 `max(0.3, 1.05 - wave*0.05 - stage*0.07 - ng*0.06)` — 스테이지1 초반은 의도적으로 초기 빌드와 동일한 페이스. 속도 배율 `1 + stage*0.05 + ng*0.1`, 회차마다 슬라임 hp `+min(2, ng)`
- **웨이브 구성** (`startWave`의 add 로직): green 2+w+s+g(≤10), bat/red는 웨이브·스테이지 게이트, sand는 s≥1(사막에선 w2부터 램프), frost s≥2, bomb s≥3, gold는 5웨이브 보스격. NG+에선 게이트 무시하고 전 종류 혼합
- **스테이지 클리어**: 보너스 `남은하트 × 40 × (1 + stage + ng*4)`, 완전 회복, HTML 오버레이(ovStage)에서 Enter/버튼으로 진행. 진행 시 지형 재생성 + 플레이어 중앙 복귀
- **게임오버 통계**: `2회차 3-4` 형식으로 도달 지점 표시

## 밸런스 상수 (조정 시 찾을 위치)
- 플레이어 피격 넉백: `hurtPlayer`의 `*140` (실측 밀림 ~20.8px; 더 줄이려면 120), 감쇠는 updPlayer의 `0.0008`(평지)/`0.08`(빙판, 실측 ~55px)
- 이동 78(P_SPEED), 공격 0.24/쿨 0.42, 대시 0.16초·속도 250·쿨 1.0·그레이스 0.10
- 몹 넉백 기본 115(hitMob), 폭발 넉백 160, 선인장 넉백 110, 용암 사출 190
- 히트스톱: 타격 0.04 / 처치 0.07 / 피격 0.08 / 투사체 파괴 0.03 / 폭발 0.06

## 검증 상태
- `harness7.js`: 36개 테스트 — 폰트 글리프, 장애물 배치 규칙(스테이지별 40회 랜덤), 빙판 관성·미끄러짐, 선인장(피해·3회 파괴·반값 점수·대시 통과), 용암(즉사·박쥐 면제·2피해 사출·무적 중 사출만), 서리 감속, 폭탄(기폭·폭발·연쇄·용암 면제), 넉백 실측 거리, 대시 용암 횡단, 스테이지 순환→2회차, 동시 출현 상한, 봇 회귀(크래시·무적 관통 감시)
- 최종 10회 연속 전부 PASS. 무적 관통(팬텀 데미지) 없음 — 38회 계측에서 최소 피격 간격 1.43초 > 무적 1.25초
- 봇은 실력이 낮아(웨이브 2~4 도달) 진행 임계값은 느슨함: 봇 관련 실패가 뜨면 크래시/minGap부터 확인할 것. "몹을 향해 대시 후 정지"는 봇의 자살 패턴이므로 봇에 넣지 말 것

## 알려진 사양 (버그 아님)
- 점프 중인 슬라임도 몸에 닿으면 접촉 피해(초기 빌드부터의 동작)
- 게임오버 화면에서 몹이 배회하지만 지형사·점수 없음(state 가드)
- 스테이지 클리어 화면을 스페이스로 넘기면 진입 직후 헛스윙 1회(무해)
- 아티팩트 환경에선 localStorage 사용 불가 — 최고기록 영속화를 원하면 파일로 직접 열 때만 동작한다는 점 유의

## 미해결 / 다음 후보 작업
1. **사용자 플레이테스트 피드백 대기 중**: 넉백 체감(140→120 여지), 스테이지별 난이도, 빙판 미끄러짐 정도, 장애물 개수
2. 아이디어 후보(미착수): 보스 몬스터, 웨이브 간 업그레이드/상점, 콤보 점수, BGM, 최고기록 저장
